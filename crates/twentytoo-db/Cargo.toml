[package]
name = "twentytoo-db"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "PostgreSQL layer for Twentytoo: embedded schema and typed access for the framework-owned tables."

[dependencies]
twentytoo-core = { path = "../twentytoo-core", version = "0.1.0" }
sqlx = { workspace = true }
chrono.workspace = true
uuid.workspace = true

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }

[lints]
workspace = true
